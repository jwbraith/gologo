
function setup() {
  var cnv = createCanvas(420, 420);
  cnv.parent('sketch');
  background(0);
  rectMode(CENTER);
}

function draw() {
  background(0);
  stroke(255);
  rect(width / 2, height / 2, 42, 32);
}